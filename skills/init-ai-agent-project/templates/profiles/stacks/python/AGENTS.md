## 技术栈规则：Python

- 以 `pyproject.toml`、锁文件和现有虚拟环境方案为准；禁止混用多个依赖管理流程。
- 新代码保持明确类型边界，数据模型和外部输入使用项目既有校验方案；不以动态字典贯穿领域层。
- 资源使用上下文管理器，异步代码不得调用阻塞 I/O；任务、超时、取消和连接清理必须闭环。
- 模块导入不得产生网络、迁移或不可逆副作用；CLI/服务入口放在显式 main 或应用工厂中。
- 测试覆盖异常类型、边界数据、时区/编码、并发与临时资源清理；固定随机性与时间依赖。
