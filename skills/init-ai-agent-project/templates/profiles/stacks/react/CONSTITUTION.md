## React 技术栈附则

- **渲染纯度：** render 不产生外部副作用，不依赖不受控的可变全局状态。
- **状态最小化：** 单一事实源；派生值即时计算，异步服务端数据不得无理由复制进全局状态。
- **副作用可终止：** 网络请求、订阅、计时器和 DOM 绑定必须处理取消、竞态与卸载清理。
- **用户级验证：** 关键交互以可访问角色和可观察结果测试，不以组件内部结构作为契约。
