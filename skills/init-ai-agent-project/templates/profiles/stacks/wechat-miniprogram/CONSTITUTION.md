## 微信小程序技术栈附则

- **平台真实：** 不把 H5 行为当作小程序行为；关键能力必须按目标基础库和真机验证。
- **数据传输节制：** `setData` 保持最小、可预测，禁止用全量同步掩盖状态设计问题。
- **授权可拒绝：** 登录、隐私、定位、相册、支付等能力必须有拒绝、取消和恢复路径。
- **包体受控：** 新依赖和资源必须评估主包、分包、启动时延与低端设备内存影响。
