## 技术栈规则：Node.js

- 以锁文件和 `engines` 为准确定 Node 与包管理器版本；不混用 ESM/CJS 或多种锁文件。
- 外部输入在 HTTP、消息、任务或 CLI 边界完成解析和校验，领域层不接受未验证对象。
- 所有 Promise 均需 await、返回或显式托管；处理超时、取消、重试、并发上限和进程退出清理。
- 依赖注入与模块边界遵循既有框架；不要通过全局单例隐藏连接、时钟或外部服务。
- 测试覆盖成功、失败、并发和资源释放；集成边界优先使用真实协议或可信测试替身。
