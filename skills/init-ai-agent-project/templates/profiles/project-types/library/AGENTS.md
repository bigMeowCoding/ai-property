## 项目形态规则：可复用库

- 公共 API、类型、导出路径和运行时行为都是兼容性边界；变更前检查真实调用方。
- 默认无导入副作用，不修改宿主全局；资源与生命周期由调用方可控。
- 依赖最小化，明确 runtime/peer/dev 归属；构建产物覆盖声明的模块格式和类型文件。
- 测试从消费者视角覆盖入口、错误、边界与声明的运行时/框架版本；避免只测内部函数。
- 破坏性变更需要版本策略、迁移说明和替代路径，不通过隐式行为变化偷渡。
