## 项目形态规则：管理端

- 权限同时检查路由、操作和服务端数据边界；前端隐藏按钮不构成授权。
- 表格、筛选、排序、分页和 URL 状态要可恢复；请求竞态不得让旧结果覆盖新条件。
- 批量、删除、发布、退款等高风险操作显示作用范围，二次确认并防止重复提交。
- 表单保留未提交状态，区分字段校验、业务冲突和网络失败；成功后刷新受影响数据。
- 审计相关操作保留操作者、对象、时间、结果和关联 ID，但不得记录敏感载荷。
- 关键流程支持键盘、清晰焦点和语义化反馈；密集信息布局不得牺牲可读性。
